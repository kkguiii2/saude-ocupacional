# ============================================================
# SAÚDE OCUPACIONAL — VARIÁVEIS DE AMBIENTE
# COPIE este arquivo para .env e preencha os valores reais.
# NUNCA versione o .env com valores reais!
# ============================================================

# -----------------------------------------------------------
# BANCO DE DADOS
# -----------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=saude_ocupacional
DB_USER=saude_user
DB_PASSWORD=<SENHA_FORTE_AQUI>

# -----------------------------------------------------------
# SERVIDOR
# -----------------------------------------------------------
SERVER_PORT=8080

# -----------------------------------------------------------
# JWT — gere com: openssl rand -hex 64
# MÍNIMO: 64 caracteres aleatórios
# -----------------------------------------------------------
JWT_SECRET=<GERE_COM_openssl_rand_-hex_64>
JWT_EXPIRATION=28800000

# -----------------------------------------------------------
# CORS — informe o(s) IP(s) do servidor de produção
# Exemplo: http://192.168.1.100:8080
# Separe múltiplos por vírgula
# -----------------------------------------------------------
CORS_ORIGINS=http://localhost:8080

# -----------------------------------------------------------
# ADMIN — senha inicial do usuário administrador
# Troque imediatamente após o primeiro login!
# -----------------------------------------------------------
ADMIN_PASSWORD=<SENHA_FORTE_AQUI>

# -----------------------------------------------------------
# PERFIL SPRING (dev | prod)
# -----------------------------------------------------------
SPRING_PROFILES_ACTIVE=prod

# -----------------------------------------------------------
# LOGS — diretório onde os arquivos de log são gravados
# -----------------------------------------------------------
LOGS=/var/log/saude-ocupacional
