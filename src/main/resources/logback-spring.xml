<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Fallback: se não existir LOGS (Render), usa /tmp -->
    <property name="LOGS" value="${LOGS:-/tmp}"/>

    <!-- ================= CONSOLE (sempre ativo) ================= -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} %-5level [%thread] %c{1}: %msg%n%throwable</pattern>
        </encoder>
    </appender>

    <!-- ================= FILE (somente se possível) ================= -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        
        <file>${LOGS}/saude-ocupacional.log</file>

        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d %p %c{1} [%t] %m%n</pattern>
        </encoder>

        <!-- CORRIGIDO (sem deprecated, sem erro de pattern) -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOGS}/archived/saude-ocupacional-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

    </appender>

    <!-- ================= ROOT ================= -->
    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </root>

    <!-- ================= SUA APP ================= -->
    <logger name="com.industrial.saude" level="DEBUG" additivity="false">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </logger>

</configuration>